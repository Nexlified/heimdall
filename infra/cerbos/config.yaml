---
server:
  httpListenAddr: ":3593"
  grpcListenAddr: ":3592"

storage:
  driver: "disk"
  disk:
    directory: /policies
    watchForChanges: true

# Enable schema enforcement
schema:
  enforcement: reject
